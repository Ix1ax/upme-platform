import {observer} from "mobx-react-lite";
import RegistrationWidget from "@/widgets/registration/RegistrationWidget";

const RegistrationPage = observer(() => {
    return (
        <RegistrationWidget />
    )
})

export default RegistrationPage;