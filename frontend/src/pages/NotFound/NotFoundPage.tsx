import {observer} from "mobx-react-lite";
import NotFoundWidget from "@/widgets/NotFound/NotFoundWidget";

const NotFoundPage = observer(() => {
    return (
        <NotFoundWidget />
    )
})

export default NotFoundPage;